package service

/*func StartReal() {
	for {
		select {
		case t := <-chans.RealChan:
			{
				//收到实时消息
				//进行分析
				if t.Message != "" {
					//转成 dayRecord格式
					r:=&data.DayRecord{}
					for _, v := range t.Data {
						r.DayBase=v.DayBase

					}
				}

			}
		default:

		}
	}
}
*/
